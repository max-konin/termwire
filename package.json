{
  "name": "termwire",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": ">=1.3.14"
  },
  "scripts": {
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "test": "bun test",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "build": "rm -rf packages/*/dist && bunx tsc -p packages/tmux/tsconfig.build.json && bunx tsc -p packages/nvim/tsconfig.build.json && bunx tsc -p packages/cli/tsconfig.build.json && bunx tsc -p packages/opencode-plugin/tsconfig.build.json && bunx tsc -p packages/mcp/tsconfig.build.json && chmod +x packages/cli/dist/bin/termwire.js packages/mcp/dist/bin/termwire-mcp.js",
    "release": "bun run build && changeset publish"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.4",
    "@changesets/cli": "^2.31.1",
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
